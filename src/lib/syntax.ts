"use client";

// Centralized Prism setup. Import only the languages we need to keep bundle small.
import Prism from "prismjs";
import "prismjs/components/prism-python";
import "prismjs/components/prism-typescript";

export { Prism };
